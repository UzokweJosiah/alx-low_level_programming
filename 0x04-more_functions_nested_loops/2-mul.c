#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers
 *
 * @a: the integer to multiply with b
 * Return: Always 0 (success)
 */

int mul(int a, int b)
{
	int num = a*b;
	_putchar(num);
	return 0;

}
