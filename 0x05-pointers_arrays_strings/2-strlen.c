#include "main.h"
#include <stdio.h>

/**
 * _strlen _ returns the lenght of a string
 * @s: string to evaluate
 *
 * Description: This function returns the lenght of a string
 * Return: the lenght of th string
 */

int _strlen(char *s)
{
	int i;

	i = 0;

	while (s[i] != '\0)
	{
		i++;
	}
	return (1);
}
