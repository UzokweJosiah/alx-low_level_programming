#include <stdio.h>
#include "main.h"
/**
 * _abs - Main Entry 
 *
 *@n: computes the absolute value of an integer
 *
 * Return: Always 0 (Success)
 */
int -abs(int)
{
	if (n < 0)
		n *= -1;
	
	return (n);
}
